[Unit]
Description=Run hive-command-agent periodically

[Timer]
# 启动后 90s 首次运行（给网络/服务留就绪时间），之后每 60s 一次
# 带 15s 抖动，避免所有节点同时打 registry
OnBootSec=90s
OnUnitActiveSec=60s
RandomizedDelaySec=15s
AccuracySec=5s

[Install]
WantedBy=timers.target
